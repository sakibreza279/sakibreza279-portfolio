import { SectionHeading } from '../../components/Headings';

const Courses = () => {
  return (
    <section>
      <SectionHeading SectionTitle='Courses' SectionSubtitle='My latest course' />
    </section>
  )
}

export default Courses;