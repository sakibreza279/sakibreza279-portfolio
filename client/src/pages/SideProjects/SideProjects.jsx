import { ContentHeading } from '../../components/Headings';

const SideProjects = () => {
  return (
    <section>
      <ContentHeading heading='Side Projects' subheading='My latest works' />
    </section>
  )
}

export default SideProjects